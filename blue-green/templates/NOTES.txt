Blue-Green Demo deployed!

Active service:  {{ template "bluegreen-demo.fullname" . }}
Preview service: {{ template "bluegreen-demo.fullname" . }}-preview

To view the active version:
  kubectl port-forward svc/{{ template "bluegreen-demo.fullname" . }} 8080:80
  open http://localhost:8080

To view the preview (green) version:
  kubectl port-forward svc/{{ template "bluegreen-demo.fullname" . }}-preview 8081:80
  open http://localhost:8081

To promote the green version to active:
  kubectl argo rollouts promote {{ template "bluegreen-demo.fullname" . }}
