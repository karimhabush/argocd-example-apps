apiVersion: v1
kind: ConfigMap
metadata:
  name: app
  labels:
    app: app
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
      <title>Pre/Post Sync</title>
      <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
          font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
          background: #f39c12;
          color: #fff;
          display: flex;
          justify-content: center;
          align-items: center;
          min-height: 100vh;
        }
        .card {
          background: rgba(255,255,255,0.15);
          backdrop-filter: blur(10px);
          border-radius: 16px;
          padding: 40px;
          max-width: 520px;
          width: 90%;
          text-align: center;
        }
        h1 { font-size: 2rem; margin-bottom: 8px; }
        .badge {
          display: inline-block;
          background: rgba(255,255,255,0.25);
          padding: 4px 16px;
          border-radius: 20px;
          font-size: 0.9rem;
          margin-bottom: 24px;
        }
        p { font-size: 1rem; line-height: 1.6; opacity: 0.9; }
      </style>
    </head>
    <body>
      <div class="card">
        <h1>Pre/Post Sync</h1>
        <span class="badge">ArgoCD Sync Hooks</span>
        <p>This app uses ArgoCD sync hooks. A PreSync job runs before deployment (e.g. DB migrations), and a PostSync job runs after (e.g. notifications). Watch the ArgoCD UI to see the execution order.</p>
      </div>
    </body>
    </html>
